type User @model
  @auth(rules: [
    { allow: groups, groups: ["Admin"] },
    { allow: owner, ownerField: "username", operations: [read] }
  ]) {
  id: ID!
  email: String!
  phone_number: String!
  full_name:String!
  alias: String!
  description: String
}
